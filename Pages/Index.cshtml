@page
@model IndexModel
@{    
    ViewData["Title"] = "Home page";
}

<div class="card mt-4">
    <div class="card-body text-center">
        <h1 class="card-title">.NET 💜 Azure</h1>
        <p class="card-text">Example .NET app to Azure App Service.</p>
    </div>
</div>
